#!/usr/bin/env bash

set -euo pipefail

question=$1
default=${2:-}
default_desc=yn

[[ -z $default || $default_desc != *$default* ]] && default=n

read -r -n1 -p "$question [${default_desc^^$default}]: " reply
echo >&2

reply=${reply:-$default}

[[ ${reply,,} == y ]]
