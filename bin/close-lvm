#!/usr/bin/env bash

set -euo pipefail

lvchange --verbose --activate n "$BOOTSTRAP_LVM_LV_ROOT_MAPPER_DEVICE"

if [[ -v BOOTSTRAP_ENABLE_SWAP ]]; then
    lvchange --verbose --activate n "$BOOTSTRAP_LVM_LV_SWAP_MAPPER_DEVICE"
fi

vgchange --verbose --activate n "$BOOTSTRAP_LVM_VG_NAME"
