#!/usr/bin/env bash

set -euo pipefail

if [[ -v BOOSTRAP_ENABLE_TRIM ]]; then
    sed -Ei '/^[[:space:]]*#[[:space:]]*issue_discards\b/ { s/#[[:space:]]*//; s/0/1/; }' \
        "$BOOTSTRAP_TARGET_MOUNT_DIR"/etc/lvm/lvm.conf
fi
