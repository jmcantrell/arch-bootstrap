#!/usr/bin/env bash

set -euo pipefail

sed -E -i '/#?[[:space:]]*\bissue_discards\b/ { s/#[[:space:]]*//; s/0/1/; }' "$BOOTSTRAP_INSTALL_MOUNT_DIR"/etc/lvm/lvm.conf

arch-chroot "$BOOTSTRAP_INSTALL_MOUNT_DIR" systemctl enable fstrim.timer
