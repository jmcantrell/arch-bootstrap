#!/usr/bin/env bash

set -euo pipefail

mkfs."$BOOTSTRAP_FS_ROOT_KIND" -L "$BOOTSTRAP_FS_ROOT_LABEL" "$BOOTSTRAP_ROOT_DEVICE"

if [[ -v BOOTSTRAP_FS_ROOT_ENABLE_SUBVOLUMES ]]; then
    create-root-subvolumes
fi
