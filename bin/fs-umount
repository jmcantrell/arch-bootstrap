#!/usr/bin/env bash

set -eu

if [[ -v INSTALL_EFI_DIR ]]; then
    umount "$INSTALL_EFI_DIR"
fi

while read -r _ path; do
    umount "$INSTALL_DIR/${path#/}"
done <"$CONFIG_DIR"/subvolumes

umount "$INSTALL_DIR"
