#!/usr/bin/env bash

set -euo pipefail

if [[ $BOOTSTRAP_BOOT_FIRMWARE == uefi ]]; then
    umount --verbose "$BOOTSTRAP_MOUNT/${BOOTSTRAP_UEFI_MOUNT#/}"
fi

while read -r _ path; do
    umount --verbose "$BOOTSTRAP_MOUNT/${path#/}"
done < <(subvolumes | tac)
