#!/usr/bin/env bash

set -eu

if [[ $INSTALL_BOOT_TYPE == uefi ]]; then
    umount "$INSTALL_DIR/${INSTALL_BOOT_UEFI_DIR#/}"
fi

while read -r _ path; do
    umount "$INSTALL_DIR/${path#/}"
done < <(list-subvolumes | tac)
