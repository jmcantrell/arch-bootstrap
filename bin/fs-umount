#!/usr/bin/env bash

set -euo pipefail

if ((${INSTALL_EFI:-0})); then
    umount /mnt/boot/efi
fi

while read -r _ mnt; do
    umount /mnt/"${mnt#/}"
done < <(tac "$BOOTSTRAP_DIR"/config/subvolumes)
