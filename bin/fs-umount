#!/usr/bin/env bash

set -eu

if [[ $INSTALL_BOOT_FIRMWARE == uefi ]]; then
    umount "$INSTALL_MOUNT/${INSTALL_UEFI_MOUNT#/}"
fi

while read -r _ path; do
    umount "$INSTALL_MOUNT/${path#/}"
done < <(list-subvolumes | tac)
