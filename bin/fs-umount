#!/usr/bin/env bash

set -euo pipefail

if ((${INSTALL_EFI:-0})); then
    umount "$INSTALL_DIR"/boot/efi
fi

while read -r _ dir; do
    umount "$INSTALL_DIR/${dir#/}"
done < <(tac "$BOOTSTRAP_DIR"/config/subvolumes)
