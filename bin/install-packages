#!/usr/bin/env bash

set -euo pipefail

pacman-key --init
pacman-key --populate
pacman --sync --refresh --needed --noconfirm archlinux-keyring

readarray -t packages < <(print-packages)

pacstrap "$BOOTSTRAP_MOUNT_DIR" "${packages[@]}"
