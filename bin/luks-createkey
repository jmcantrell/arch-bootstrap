#!/usr/bin/env bash

set -euo pipefail

keyfile=$BOOTSTRAP_MOUNT_DIR/${BOOTSTRAP_LUKS_KEYFILE_PATH#/}

rm -f "$keyfile"
mkdir -p "$(dirname "$keyfile")"

umask a=

dd bs=512 count=4 if=/dev/random of="$keyfile" iflag=fullblock

printf "%s" "$keyfile"
