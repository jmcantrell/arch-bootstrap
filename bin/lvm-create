#!/usr/bin/env bash

set -euo pipefail

pvcreate "$INSTALL_LVM_DEVICE"
vgcreate vg "$INSTALL_LVM_DEVICE"
lvcreate --size "$INSTALL_SWAP_SIZE" vg --name swap
lvcreate --extents "+100%FREE" vg --name root
