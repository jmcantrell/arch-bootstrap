#!/usr/bin/env bash

set -euo pipefail

pvcreate -ff --yes "$INSTALL_LVM_DEVICE"
vgcreate vg "$INSTALL_LVM_DEVICE"
lvcreate --wipesignatures y --yes --size "$INSTALL_SWAP_SIZE" vg --name swap
lvcreate --wipesignatures y --yes --extents "+100%FREE" vg --name root
