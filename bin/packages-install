#!/usr/bin/env bash

set -euo pipefail

pacman --sync --refresh --needed --noconfirm archlinux-keyring

# Enable parallel package downloads during installation.
sed -i '/^\s*#\s*ParallelDownloads\b/s/\s*#\s*//' /etc/pacman.conf

list-packages | pacstrap "$INSTALL_MOUNT" -
