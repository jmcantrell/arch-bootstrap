#!/usr/bin/env bash

set -eu

pacman -Sy --needed --noconfirm archlinux-keyring

# Enable parallel package downloads during installation.
sed -i '/^#\?ParallelDownloads\b/s/#//' /etc/pacman.conf

pacstrap "$INSTALL_MOUNT" - < <(list-packages)
