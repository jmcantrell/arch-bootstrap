#!/usr/bin/env bash

set -eu

parted -s "$INSTALL_DEVICE" \
    mklabel gpt \
    mkpart "$INSTALL_BOOT_PART_NAME" 0% "$INSTALL_BOOT_PART_SIZE" \
    mkpart "$INSTALL_OS_PART_NAME" "$INSTALL_BOOT_PART_SIZE" "$INSTALL_OS_PART_SIZE" \
    set 1 "$INSTALL_BOOT_PART_FLAG" on \
    set 2 lvm on
