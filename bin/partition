#!/usr/bin/env bash

set -euo pipefail

sfdisk "$BOOTSTRAP_INSTALL_DEVICE" <<EOF
label: gpt
size=$BOOTSTRAP_PART_BOOT_SIZE, type=$BOOTSTRAP_PART_BOOT_TYPE, name=$BOOTSTRAP_PART_BOOT_NAME
size=$BOOTSTRAP_PART_SYS_SIZE,  type=$BOOTSTRAP_PART_SYS_TYPE,  name=$BOOTSTRAP_PART_SYS_NAME
EOF
