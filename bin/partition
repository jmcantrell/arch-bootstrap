#!/usr/bin/env bash

set -eu

parted -s "$INSTALL_DEVICE" \
    unit MiB \
    mklabel gpt \
    mkpart boot 0% "$INSTALL_BOOT_PARTITION_SIZE" \
    mkpart os "$INSTALL_BOOT_PARTITION_SIZE" 100% \
    set 1 "$INSTALL_BOOT_PARTITION_FLAG" on \
    set 2 lvm on
