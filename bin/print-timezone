#!/usr/bin/env bash

set -euo pipefail

default=${1:-UTC}

link=/etc/localtime

if [[ -f $link ]]; then
    file=$(realpath "$link")
    name=${file#*/zoneinfo/}
else
    name=$default
fi

printf "%s" "$name"
