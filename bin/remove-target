#!/usr/bin/env bash

set -euo pipefail

if [[ -v BOOTSTRAP_ENABLE_LVM ]]; then
    remove-lvm-root
    if [[ -v BOOTSTRAP_ENABLE_SWAP ]]; then
        remove-lvm-swap
    fi
    remove-lvm
fi

if [[ -v BOOTSTRAP_ENABLE_LUKS ]]; then
    remove-luks
fi
