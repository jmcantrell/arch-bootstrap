export INSTALL_DEVICE=/dev/sda

export INSTALL_SWAP_SIZE=4G

# Should full disk encryption be used?
# export INSTALL_LUKS_PASSPHRASE=hunter2

export INSTALL_HOSTNAME=arch

# export INSTALL_LANG=en_US.UTF-8
# export INSTALL_KEYMAP=us
# export INSTALL_FONT=Lat2-Terminus16
# export INSTALL_TIMEZONE=UTC

# Used by reflector to filter pacman mirrors.
# If unset, the value will be parsed from INSTALL_LANG.
# export INSTALL_COUNTRY=US

# Allow the wheel group password-less sudo?
# export INSTALL_SUDO_NOPASSWD=1

export INSTALL_SUDOER_USERNAME=user
export INSTALL_SUDOER_PASSWORD=hunter2
# export INSTALL_SUDOER_SHELL=/bin/zsh  # be sure it's in ../packages

# Should the display go to sleep after a bit?
# export INSTALL_CONSOLE_BLANK=600  # 10 minutes

# vi:ft=sh
